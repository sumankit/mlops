# Copy this file to .env and fill in real values before switching to PostgreSQL.
# The pipeline currently defaults to SQLite (no credentials needed) — see src/config.py.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=student_performance
DB_USER=your_db_user
DB_PASSWORD=your_db_password
